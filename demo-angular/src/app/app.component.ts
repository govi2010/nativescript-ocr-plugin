import { Ocr } from 'nativescript-ocr';
console.log(new Ocr().message);
import { Component } from "@angular/core";

@Component({
    moduleId: module.id,
    selector: "ns-app",
    templateUrl: "app.component.html"
})
export class AppComponent { }
